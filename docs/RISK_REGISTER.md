# RISK REGISTER — AEGIS-X PRIME

Severity × likelihood → priority. "Owner" = who can retire the risk. Review each phase gate.

| ID | Risk | Sev | Lik | Priority | Mitigation | Owner |
|----|------|-----|-----|----------|------------|-------|
| R-1 | **No project license** — repo is technically all-rights-reserved; any packaging/redistribution is blocked | High | Certain | **P0 blocker** | License decision record in Phase 0; owner chooses; block release artifacts until then | Project owner |
| R-2 | **NLLB weights are CC-BY-NC-4.0** — the shipped Arabic-translation stage is non-commercial-only | High | Certain | **P0 blocker (decision)** | Record in MODEL_LICENSES.yaml; deployment-posture decision; evaluate permissive MT alternatives in Phase 8; license-gate the adapter per profile | Project owner |
| R-3 | Second ASR family (Omnilingual ASR) may have unverified he/fa quality, license, or Windows support | High | Med | High | Fail-closed startup verification of language coverage; license check before download; OWSM-CTC as challenger; mock-first adapter | Eng + owner approval |
| R-4 | 8 GB VRAM ceiling — forest + fusion may not fit even sequentially; WDDM shared-memory spill silently destroys throughput (observed pattern documented in summary.py comments) | High | Med | High | Strict one-model residency; stage-oriented batching (P12); VRAM watermarks in doctor; measured RTF gates before enabling any new model in fast profile | Eng |
| R-5 | Evaluation data scarcity for degraded he/fa — without it, calibration (P6) and acceptance gates (§16.9) are impossible, and no accuracy claims can be made at all | High | High | High | Start data acquisition track at P0 in parallel; synthetic degradation as stopgap clearly labeled; forbid accuracy claims until benchmark runner produces them (spec §1) | Owner + Eng |
| R-6 | Hallucination risk shifts rather than disappears — fusion + keyword rescue can *introduce* confident wrong text if diversity penalty/abstention are mistuned | High | Med | High | Hallucinated-words-per-non-speech-minute metric in every eval; abstention-first defaults; insertion-rate regression tests on silence fixtures | Eng |
| R-7 | Windows-specific fragility (symlinks, torchcodec DLLs, cp1256 consoles, Rust-less builds, PowerShell 5.1 quoting) breaks new deps | Med | High | High | Keep guard-utilities pattern; CI on Windows runner; WSL/Docker path documented as alternative without breaking PS scripts (spec §6.14) | Eng |
| R-8 | **Repo lives inside OneDrive** — sync locks/latency corrupt SQLite WAL and large artifact writes; also uploads potentially sensitive audio to cloud, conflicting with the local-only posture | Med→High | High | High | Phase 12 artifact store + DB on non-synced local path; document in OPERATIONS runbook; consider moving the working tree itself (operator decision — touches their workflow) | Operator |
| R-9 | GPU/toolchain drift — setup.ps1 still installs cu124 torch that cannot run on this sm_120 GPU; a fresh setup silently reproduces the broken state | High | Certain (on re-setup) | **P0 fix** | Update setup.ps1/requirements + doctor check that fails loudly when arch list lacks the device's compute capability | Eng |
| R-10 | Model-revision drift — all HF references float on `main`; upstream changes alter behavior/licenses silently | Med | Med | Med | MODEL_MANIFEST.yaml pinning + hash verification (P0/P13) | Eng |
| R-11 | Schema migration debt — existing report.json consumers (UI, decks) break when the evidence schema lands | Med | High | Med | `report_schema_version`, migration utility, old-report compatibility tests (P9) | Eng |
| R-12 | Scope explosion — the spec describes months of work; hackathon timelines force cuts that could land in unsafe places (e.g., skipping calibration but keeping confidence displays) | Med | High | High | Phase gates with explicit DoD; if cut, cut whole phases and keep raw_model_score labeling honest; never ship uncalibrated numbers as "confidence" | Owner + Eng |
| R-13 | Secrets/PII leakage — HF token pasted into chat/UI during this session; personal recordings processed on this machine | Med | Med | Med | Token rotation recommended (already advised); PRIVACY.md + retention policy (P0); logs redact tokens; outputs/ and uploads/ stay gitignored (verified) | Operator + Eng |
| R-14 | Dual-use sensitivity — degraded-comms transcription with military terminology has misuse potential; spec itself mandates legal data only | Med | Low | Med | Enforce spec §1 data rules; PRIVACY/SECURITY docs state authorized-use posture; no capability marketing ("military-grade" language banned by spec §24) | Owner |
| R-15 | Emotion/summary misinterpretation by end users (treated as ground truth) | Med | Med | Med | Default-off emotion, deterministic evidence-linked summary, UI "experimental/inference" labels (P9; partially done for summary toggle) | Eng |
| R-16 | Cooperative cancellation gaps — a hung native call (CUDA, ffmpeg) ignores cancel checks; observed server death mid-batch predates watchdogs | Med | Med | Med | Stage timeouts + watchdog + quarantine in P12 reliability work; server.log + faulthandler already landed (uncommitted) | Eng |
| R-17 | Licensing of *outputs* — translations produced via NC-licensed models may constrain downstream use of outputs in commercial settings | Med | Low–Med | Med | Legal review alongside R-2; per-report license stamp listing models used | Owner |
